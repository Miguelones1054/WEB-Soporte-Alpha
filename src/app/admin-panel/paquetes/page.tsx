'use client';

import { HubSectionRedirect } from '../views/HubSectionRedirect';

export default function PaquetesPage() {
  return <HubSectionRedirect section="paquetes" message="Abriendo paquetes..." />;
}
